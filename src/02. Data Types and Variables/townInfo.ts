function townInfo(townName: string, population: number, area: number) {
  const result = `Town ${townName} has population of ${population} and area ${area} square km.`;

  console.log(result);
}

townInfo("Sofia", 1286383, 492);
